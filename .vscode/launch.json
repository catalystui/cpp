{
    "configurations": [
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Debug (gdb)",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceRoot}"
        },
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Debug (lldb)",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceRoot}"
        }
    ]
}
