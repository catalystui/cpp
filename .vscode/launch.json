{
    "configurations": [
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Debug (gdb)",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceRoot}"
        }
    ]
}
